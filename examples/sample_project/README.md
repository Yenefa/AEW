# AEDL-Demo

**Autonomous Engineering Design Loop · 自主嵌入式工程研发闭环**

北极星：让嵌入式工程从设计到验证全自动闭环。

## 这是什么

一个用于演示 AEW Terminal Decision Layer 的最小示例项目。结构与真实 AEDL 一致，
但内容精简，方便离线跑通 `python -m aew.cli` 与 `agent` REPL。
